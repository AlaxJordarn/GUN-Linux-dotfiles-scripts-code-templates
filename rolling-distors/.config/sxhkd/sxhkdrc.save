#
# wm independent hotkeys
#

# terminal emulator
super + Return
	termite  
#urxvt

super + t
    termite
ctrl + alt + t
    urxvt 
# program launcher
super + @space
	dmenu_run
super + e
    pcmanfm
super + y
    gnome-calculator
#browsers 
super + shift + ctrl + q 
     exec sh /home/"$(whoami)"/pm.sh
super + shift + w 
     sh /home/"$(whoami)"/sf.sh
#bspc node -t tiled
super + shift + q
 exec urxvt -e firejail w3m -no-cookie -cols -num -4 www.duckduckgo.com 
# rofi
super + r 
    rofi -show drun

# make sxhkd reload its configuration files:
super + shift + r
	pkill -USR1 -x sxhkd

#
# bspwm hotkeys
#

# quit/restart bspwm
super + alt + {q,r}
	bspc {quit,wm -r}

# close and kill
alt + F4
	bspc node -{c,k}
#super + q 
 #   bspc node -k
     
# alternate between the tiled and monocle layout
super + f
	bspc desktop -l next

# send the newest marked node to the newest preselected node
super + y
	bspc node newest.marked.local -n newest.!automatic.local

# swap the current node and the biggest node
super + g
	bspc node -s biggest

#
# state/flags
#

# set the window state
super + shift + space 

	bspc node -t {tiled,pseudo_tiled,floating}


# set the node flags
super + ctrl + {m,x,y,z}
	bspc node -g {marked,locked,sticky,private}

#
# focus/swap
#

# focus the node in the given direction
#super + {_,shift + }{h,j,k,l}
#	bspc node -{f,s} {west,south,north,east}
super  {h,j,k,l}
	bspc node -s {west,south,north,east}
alt + {h,j,k,l}
    bspc node -f {west,south,north,east}
# focus the node for the given path jump
super + {p,b,comma,period}
	bspc node -f @{parent,brother,first,second}

# focus the next/previous node in the current desktop
alt + Escape
	bspc node -f {next}.local 
super + Tab
    bspc node -f {next}.local
# focus the next/previous desktop in the current monitor
super + {n,m}
	bspc desktop -f {prev,next}.local
super + shift + {n,m}
    bspc node -d {prev,next}  
ctrl + alt + {Left,Right}
	bspc desktop -f {prev,next}.local
shift + alt + {Left,Right} 
    bspc node -d {prev,next}  
# send to next 
super + shift + {n,m}
    bspc {desktop -f,node -d} '^{prev,next}.local'
# focus the last node/desktop
super + {grave,Tab}
	bspc {node,desktop} -f last

# focus the older or newer node in the focus history
super + {o,i}
	bspc wm -h off; \
	bspc node {older,newer} -f; \
	bspc wm -h on

# focus or send to the given desktop
super + {_,shift + }{1-9,0}
	bspc {desktop -f,node -d} '^{1-9,10}'

#
# preselect
#

# preselect the direction
super + alt + {h,j,k,l}
	bspc node -p {west,south,north,east}

# preselect the ratio
super + ctrl + {1-9}
	bspc node -o 0.{1-9}

# cancel the preselection for the focused node
super + ctrl + space
	bspc node -p cancel

# cancel the preselection for the focused desktop
super + ctrl + shift + space
	bspc query -N -d | xargs -I id -n 1 bspc node id -p cancel

#
# move/resize
#

# expand a window by moving one of its side outward
super + shift + {h,j,k,l}
	bspc node -z {left -20 0,bottom 0 20,top 0 -20,right 20 0}

# contract a window by moving one of its side inward
alt + shift + {h,j,k,l}
	bspc node -z {right -20 0,top 0 20,bottom 0 -20,left 20 0}

#alt + shift + {h,j,k,l}
#	bspc node -z {right -20 0 || left -20 0,top 0 20 || bottom 0 20,bottom 0 -20 || top 0 -20 ,left 20 0 || right 20 0}


# expand + contrast
#super + ctrl + {h,j,k,l}
#    bspc node -z {left -
# move a floating window
#super + {Left,Down,Up,Right}
shift + alt  + {h,j,k,l}
	bspc node -v {-20 0,0 20,0 -20,20 0}

#audio 
F12 
   amixer set Master 2%+ unmute
F11 
   amixer set Master 2%- unmute
F10
   amixer set Master toggle
#backlights
F5 
  xbacklight -dec 0.5

F6 
  xbacklight -inc 0.5

XF86MonBrightnessUp 
 xbacklight -inc 0.2

XF86MonBrightnessDown 
 xbacklight -dec 0.2
#screen shot
ctrl+@Print 
      {scrot -s -Z 'ArcoLinux_%Y-%m-%d-%S_$wx$h.png' -e 'mv $f $$(xdg-user-dir PICTURES)' }
#XF86Display exec scrot -s 'ArcoLinux_%Y-%m-%d-%S_$wx$h.png' -e 'mv $f $$(xdg-user-dir PICTURES)'

#xrandr 
super + ctrl + alt + shift + F8 
    xrandr --output eDP1 --mode 1920x1080 --rate 59.93 &&    exec nitrogen --restore
super + ctrl + alt + shift + F5 
    xrandr --output eDP1 --mode 1280x720 --rate 59.74 && exec  nitrogen --restore
super + ctrl + alt + shift + F9 
    xrandr --output eDP1 --mode 2048x1152 --rate 59.91  && exec  nitrogen --restore

super + ctrl + alt + shift + F6 
 xrandr --output eDP1 --mode 1368x768 --rate 59.8 && exec   nitrogen --restore

super + ctrl + alt + shift + F7 
   xrandr --output eDP1 --mode 1600x900 --rate 35.00 && exec   nitrogen --restore
